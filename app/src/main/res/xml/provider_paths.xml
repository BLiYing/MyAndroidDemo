<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!--<external-path name="external_files" path="."/>-->

    //等同于Context.getExternalFilesDir(null).
    <external-files-path name="apk" path="Download/apk/"/>
    //等同于Represents files in the root of your app's external cache area. The root path of this subdirectory
    //is the same as the value returned by Context.getExternalCacheDir().
    <external-cache-path name="myimage" path="images/" />

    <!--Context.getCacheDir()-->
    <cache-path name="name" path="cachepath" />

    <!--等同于getExternalStorageDirectory-->
    <external-path
        name="my_images"
        path="Pictures"/>
    <files-path name="files" path="."/>

</paths>